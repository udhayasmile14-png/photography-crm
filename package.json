{
  "name": "photography-crm-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "uvicorn main:app --app-dir backend --reload --port 8000",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently --kill-others -n \"API,APP\" -c \"magenta,cyan\" \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
